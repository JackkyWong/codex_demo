# Demo Spring Boot 项目

这是一个最小可运行的 Spring Boot 项目，包含：

- `spring-boot-starter-web`
- `spring-boot-starter-test`
- 示例接口 `GET /`，返回 `Hello, Spring Boot!`

## 运行

```bash
mvn spring-boot:run
```

## 测试

```bash
mvn test
```
